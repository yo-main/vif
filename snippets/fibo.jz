def fib(n):
    if n <= 1:
        return n
    return fib(n-2) + fib(n-1)


var i = 0
while True:
    # var start = get_time()
    var value = fib(i)
    # var duration = (get_time() - start) / 1000000

    @value

    # print("Got ", value, "in ", duration, "seconds")

    i = i + 1


